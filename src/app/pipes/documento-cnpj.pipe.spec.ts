import { DocumentoCNPJPipe } from './documento-cnpj.pipe';

describe('DocumentoCNPJPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentoCNPJPipe();
    expect(pipe).toBeTruthy();
  });
});
