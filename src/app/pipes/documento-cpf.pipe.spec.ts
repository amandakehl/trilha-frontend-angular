import { DocumentoCPFPipe } from './documento-cpf.pipe';

describe('DocumentoCPFPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentoCPFPipe();
    expect(pipe).toBeTruthy();
  });
});
